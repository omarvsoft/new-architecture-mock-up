server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/omarvsoft/new-architecture-mock-up.git
          username: muck-up
          password: co-qui2017
          search-paths:
            - config-repository